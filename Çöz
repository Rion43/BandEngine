I believe we are no longer debugging authentication.

Authentication is already COMPLETE.

Confirmed facts:

✔ SessionConfig succeeds.
✔ PhoneNonce exchange succeeds.
✔ WatchNonce exchange succeeds.
✔ AuthStep3 succeeds.
✔ AUTH SUCCESS is received.
✔ If NO encrypted packets are sent afterwards, the band still disconnects after about 70-100ms.

Therefore:

- HKDF is correct.
- AES is correct.
- Protobuf is correct.
- SessionConfig is correct.
- Authentication is correct.

Do NOT spend time on cryptography.

========================================================

I believe the problem is inside the Bluetooth lifecycle.

Please prove or disprove the following hypothesis.

========================================================

Hypothesis:

BandEngine does not reproduce the Android BluetoothGatt lifecycle that Gadgetbridge relies on.

Possible missing behaviors include:

1.
TransactionBuilder callback ordering.

Android:

write
↓

onCharacteristicWrite
↓

next write

BandEngine:

writeWithoutResponse
↓

Promise resolved
↓

next write

Determine whether any Xiaomi code depends on write callbacks.

--------------------------------------------------------

2.
Descriptor callback ordering.

Android:

setCharacteristicNotification()

↓

write CCCD descriptor

↓

onDescriptorWrite()

↓

continue

Does XiaomiBleProtocolV2 assume that onDescriptorWrite has completed before authentication begins?

--------------------------------------------------------

3.
AUTH SUCCESS callback chain.

Trace EVERY executed function after AUTH SUCCESS until the first normal encrypted command.

Produce the complete call graph.

Search for:

Handler
Runnable
Executor
postDelayed
Timer
Queue
TransactionBuilder
Callbacks

I suspect an initialization task exists that BandEngine never performs.

--------------------------------------------------------

4.
Disconnect handling.

I need evidence.

Does Gadgetbridge ever receive an immediate disconnect after AUTH SUCCESS?

If yes:

show the exact code path.

show the reconnect path.

show how encryption state survives reconnect.

If not:

prove that the connection stays alive.

Do NOT speculate.

--------------------------------------------------------

5.
Reconnect.

Xiaomi documentation explicitly expects automatic reconnect behavior.

Determine whether XiaomiBleProtocolV2 relies on reconnect after authentication.

If reconnect is part of the expected lifecycle,
show exactly how Gadgetbridge restores the encrypted session.

--------------------------------------------------------

6.
MTU.

Determine whether requestMtu(512) affects anything after authentication.

Trace every usage of:

maxWriteSize

Determine whether authentication packets,
SPPv2 packets,
or only file transfers depend on MTU.

--------------------------------------------------------

7.
Search the repository for ANY packet sent automatically after AUTH SUCCESS.

I want every packet between:

AUTH SUCCESS

and

Clock synchronization.

List:

protobuf type
subtype
purpose
call stack

--------------------------------------------------------

8.
Search for hidden keepalive packets,
ACK packets,
heartbeat packets,
or session confirmation packets.

Ignore protobuf decoding.

Only search for lifecycle behavior.

========================================================

Important:

Do not answer with theories.

Only output findings that are backed by source code.

For every finding provide:

- filename
- function
- line numbers
- complete execution path

If something cannot be proven from source code,
explicitly say "NOT PROVEN".

========================================================

I strongly suspect the bug is NOT inside authentication anymore.

I suspect it is inside the Bluetooth lifecycle immediately after AUTH SUCCESS.

Please try to prove me wrong.